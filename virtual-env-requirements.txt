numpy
matplotlib
pytest
scipy

# The A=1 framework engine is provided by dcl_core's `core` submodule
# (Paper~I's continuous-amplitude engine, ported verbatim into
# dcl_core).  Pinned to dcl_core v0.1.0
# (Zenodo: 10.5281/zenodo.20350952) so this paper's Zenodo deposit
# anchors to an immutable engine version.  Bump the @vX.Y.Z tag here
# whenever dcl_core ships a new release that this paper should
# rebuild against.
dcl_core @ git+https://github.com/JackDMenendez/dcl-core@v0.1.0

# scipy is used by the experimental analysis in
# src/experiments/exp_18_tidal_ionization.py (curve fitting of the
# M_min(d) ionization threshold against the predicted Roche-like
# d^3 scaling).

# Pin specific versions in a separate file (e.g. requirements.lock.txt)
# once the environment is stable.  Add other dependencies as the project
# develops -- e.g. sympy, networkx, pandas, seaborn -- but keep
# this file minimal so a fresh clone can install quickly.
